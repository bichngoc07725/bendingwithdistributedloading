numpy>=1.26
matplotlib>=3.8
torch>=2.2
# 0.38.0 is the latest PennyLane + Lightning pair currently available for the bundled
# Python 3.9 runtime. The used APIs (TorchLayer, AngleEmbedding and
# StronglyEntanglingLayers) are supported by this version.
pennylane==0.38.0
# PennyLane 0.38 requires the pre-0.7 autoray API (NumpyMimic).
autoray==0.6.12
